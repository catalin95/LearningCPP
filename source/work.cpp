#include "work.hpp"

auto func(const int32_t num) -> int32_t
{
    if (num > 0)
        return num; 
    else
        return 0;
}

auto func2(const int32_t num) -> int32_t
{
    if (num > 0)
        return num; 
    else
        return 0;
}
